<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.divy</groupId>
	<artifactId>org.divy.bo</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>org.divy.bo</name>

	<modules>
		<module>bo-framework-core</module>
		<module>bo-framework-database</module>
		<module>bo-framework-database-mock</module>
		<module>bo-framework-service</module>
	</modules>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<sonar.dynamicAnalysis>false</sonar.dynamicAnalysis>
		<sonar.scm.url>scm:git:http://divyakumar.dyndns.org/git/git/org.divy.task.git</sonar.scm.url>
		<VERSION_NUMBER>0.0.1-SNAPSHOT</VERSION_NUMBER>
		<releaseRepoUrl>http://divyakumar.dyndns.org/nexus/content/repositories/releases/</releaseRepoUrl>
		<snapshotRepoUrl>http://divyakumar.dyndns.org/nexus/content/repositories/snapshots/</snapshotRepoUrl>
		<deployRepoUrl>${releaseRepoUrl}</deployRepoUrl>
		<isRelease>true</isRelease>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.0</version>
				<configuration>
					<source>1.7</source>
					<target>1.7</target>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.4.3</version>
				<executions>
					<execution>
						<id>replace-pom-placeholder</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<resources>
								<resource>
									<directory>${basedir}</directory>
									<includes>
										<include>pom.xml</include>
									</includes>
									<filtering>true</filtering>
								</resource>
							</resources>
							<outputDirectory>${project.build.directory}/pom-install-deploy-fix</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<artifactId>maven-install-plugin</artifactId>
				<version>2.4</version>
				<executions>
					<execution>
						<id>overwrite-pom</id>
						<phase>install</phase>
						<goals>
							<goal>install-file</goal>
						</goals>
						<configuration>
							<packaging>pom</packaging>
							<file>target/pom-install-deploy-fix/pom.xml</file>
							<pomFile>target/pom-install-deploy-fix/pom.xml</pomFile>
							<version>${project.version}</version>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.5</version>
				<executions>
					<execution>
						<id>overwrite-pom</id>
						<phase>deploy</phase>
						<goals>
							<goal>deploy-file</goal>
						</goals>
						<configuration>
							<packaging>pom</packaging>
							<file>target/pom-install-deploy-fix/pom.xml</file>
							<pomFile>target/pom-install-deploy-fix/pom.xml</pomFile>
							<url>${deployRepoUrl}</url>
							<version>${project.version}</version>
							<updateReleaseInfo>${isRelease}</updateReleaseInfo>
							<uniqueVersion>false</uniqueVersion>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-scm-plugin</artifactId>
				<version>1.4</version>
				<configuration>
					<tag>${project.artifactId}-0.0.1-SNAPSHOT</tag>
				</configuration>
			</plugin>
			<!-- To use the plugin goals in your POM or parent POM -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>versions-maven-plugin</artifactId>
			</plugin>
		</plugins>
		<!-- To define the plugin version in your parent POM -->
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>versions-maven-plugin</artifactId>
					<version>2.0</version>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<scm>
		<connection>scm:git:http://divyakumar.dyndns.org/git/git/org.divy.task.git</connection>
		<url>scm:git:http://divyakumar.dyndns.org/git/git/org.divy.task.git</url>
		<developerConnection>scm:git:http://divyakumar.dyndns.org/git/git/org.divy.task.git</developerConnection>
	</scm>
	<repositories>
		<repository>
			<id>divyakumar.dyndns.org</id>
			<name>divyakumar.dyndns.org</name>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<url>http://divyakumar.dyndns.org/nexus/content/groups/public/</url>
		</repository>
	</repositories>
	<distributionManagement>
		<repository>
			<id>release.divyakumar.dyndns.org</id>
			<url>${releaseRepoUrl}</url>
		</repository>
		<snapshotRepository>
			<id>snapshot.divyakumar.dyndns.org</id>
			<name>Internal Releases</name>
			<url>${snapshotRepoUrl}</url>
		</snapshotRepository>
	</distributionManagement>
	<profiles>
		<profile>
			<id>snapshot-deploy-url-override</id>
			<activation>
				<property>
					<name>!VERSION_NUMBER</name>
				</property>
			</activation>
			<properties>
				<deployRepoUrl>${snapshotRepoUrl}</deployRepoUrl>
				<isRelease>false</isRelease>
			</properties>
		</profile>
	</profiles>
	<reporting>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>versions-maven-plugin</artifactId>
				<version>2.0</version>
			</plugin>
		</plugins>
	</reporting>
</project>
